<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage paths for recordings -->
    <external-path name="external_storage" path="." />
    
    <!-- Application external storage -->
    <external-files-path name="external_files" path="." />
    
    <!-- Gallery and recordings directory -->
    <external-path name="gallery" path="TopInfrared/" />
    
    <!-- Enhanced recordings directory -->
    <external-path name="enhanced_recordings" path="TopInfrared/enhanced_recordings/" />
    
    <!-- GSR data directory -->
    <external-path name="gsr_data" path="TopInfrared/gsr_data/" />
    
    <!-- Cache directory for temporary files -->
    <cache-path name="cache" path="." />
</paths>